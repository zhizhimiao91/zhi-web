# zhi-web 全栈文章站 · 总汇文档

> 版本：v1.0（2026-08-20）
> 目标：个人知识库/文章站，纯静态部署在 GitHub Pages 子域名，后端 API 跑在阿里云服务器。

---

## 一、整体架构

```mermaid
graph TD
    A["用户浏览器"] --> B["GitHub Pages 子域名<br>(zhizhimiao91.github.io, 自定义子域名CNAME)"]
    B --> C["静态 HTML/JS/CSS<br>(Vike SSG 产物 dist/client/)"]
    C --> D["/api/* 请求"]
    D --> E["阿里云服务器 2核2G"]
    E --> F["nginx 反代 /api"]
    F --> G["Hono 后端 (127.0.0.1:3000)"]
    G --> H["SQLite / 本地文件存储"]
```

### 三层职责

| 层 | 承载方 | 内容 | 技术 |
|----|--------|------|------|
| 前端静态层 | GitHub Pages | 全部文章页面（SSG 生成的 HTML + 打包 JS/CSS） | Vike + Vue3 + Vite7 |
| 后端 API 层 | 阿里云服务器 | /api 下的动态接口（评论、搜索、计数、管理接口） | Hono (TypeScript) |
| 内容源 | GitHub 仓库 content/ 目录 | Markdown 文章，push 后 Actions 自动构建发布 | Git |

---

## 二、仓库结构

```
zhi-web/
├── .github/
│   └── workflows/
│       └── deploy.yml        # 前端构建 + 发布到 Pages
├── frontend/                 # Vike + Vue3 前端（SSG）
│   ├── package.json
│   ├── vite.config.ts
│   ├── pages/                # 文件路由（+Page.vue / +route.ts）
│   ├── renderer/             # +onRenderHtml / +onRenderClient
│   └── dist/client/          # 构建产物（不提交 git）
├── server/                   # Hono 后端
│   ├── package.json
│   ├── src/
│   │   ├── index.ts          # Hono 入口，监听 127.0.0.1:3000
│   │   ├── routes/           # /api 路由
│   │   └── db.ts             # SQLite/存储层
│   └── ecosystem.config.js   # pm2 配置
├── content/                  # 文章 Markdown 源（唯一内容入口）
│   └── <category>/<slug>.md
├── .gitignore
└── README.md
```

---

## 三、路由表

| 路径 | 类型 | 说明 | 前端/后端 |
|------|------|------|-----------|
| `/` | 页面 | 首页/文章列表 | 前端 SSG |
| `/page/:category` | 页面 | 分类文章列表 | 前端 SSG |
| `/page/:category/:slug` | 页面 | 文章详情页（正文渲染） | 前端 SSG |
| `/api/articles` | 接口 | 文章列表 JSON | 后端 Hono |
| `/api/articles/:slug` | 接口 | 单篇文章详情 | 后端 Hono |
| `/api/search?q=` | 接口 | 站内搜索 | 后端 Hono |
| `/api/health` | 接口 | 健康检查 | 后端 Hono |

- 前端用 **history 模式**，SSG 构建时按路由生成真实 HTML（`/page/xxx/index.html`），SEO 完整、可直接被爬虫读取。
- 后端接口前缀统一 `/api`，与前端分离，CORS 只放行文章域名。

---

## 四、GitHub Actions 工作流（deploy.yml）

```yaml
# 触发：push 到 main 分支
# 作用：安装依赖 → SSG 构建（vike build）→ 产物部署到 GitHub Pages
```

### 关键步骤与官方 Action

| 步骤 | Action | 说明 |
|------|--------|------|
| 检出代码 | actions/checkout@v4 | 拉取仓库（含 content/ 文章） |
| 安装 Node | actions/setup-node@v4 | Node 22 |
| 构建前端 | npm ci + npm run build | 生成 dist/client/（含 .nojekyll） |
| 配置 Pages | actions/configure-pages@v5 | 设置 Pages 发布参数 |
| 上传产物 | actions/upload-pages-artifact@v3 | 上传 dist/client |
| 发布 | actions/deploy-pages@v4 | 发布到 Pages |

### GitHub 仓库设置（一次性）

- [x] Actions → General → Workflow permissions → **Read and write permissions**
- [x] Pages → Source → **GitHub Actions**
- [x] Actions permissions → **Allow all actions**（只用官方 action，安全）

### 产物路径细节

- Vike 构建产物在 `frontend/dist/client/`
- **必须放置空文件 `.nojekyll`**，否则 `_` 开头的静态资源不被 GitHub Pages 服务
- 子域名绑定根路径，Base URL 保持 `/`，无需额外 base 配置

---

## 五、部署链路（日常发布流程）

### 写文章

```bash
# 1. 新建文章
content/技术/vue响应式原理.md

# 2. 提交推送
git add .
git commit -m "feat: 新增文章《Vue 响应式原理》"
git push
```

push 后 **自动**：Actions 检测 content/ 变更 → 构建 → 文章页面上线。无需手动部署。

### 改代码（前端/后端）

```bash
git add .
git commit -m "feat: 增加 xxx 功能"
git push        # 前端自动发布 Pages
git push        # 后端代码需再 ssh 到服务器拉取重启
```

### 后端上线（首次 + 更新）

```bash
# 服务器端（一次性初始化）
ssh root@<服务器IP>
git clone https://github.com/zhizhimiao91/zhi-web.git
cd zhi-web/server && npm ci && npm run build

# pm2 守护
npm i -g pm2
pm2 start ecosystem.config.js
pm2 save && pm2 startup

# 更新流程
cd zhi-web && git pull && cd server && npm run build && pm2 restart zhi-web-api
```

### nginx 反代（服务器端，一次性配置）

```nginx
server {
    listen 80;
    server_name api.你的域名.com;   # 或放在主站 server 块内

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 六、域名与 DNS 规划

| 域名 | 指向 | 用途 |
|------|------|------|
| `你的主域名.com` | 阿里云 ESA → 服务器 | 主站入口 / 后端 |
| `文章子域名.com`（如 blog.xxx.com） | CNAME → `zhizhimiao91.github.io` | 前端静态站点（GitHub Pages） |

> 注意：GitHub Pages 国内访问不稳定（Fastly CDN，DNS 间歇干扰）。后续可用 ESA 把文章子域名做回源到 GitHub Pages 的兜底。

### 后端 CORS 配置（server/src/index.ts 内）

```ts
app.use('/api/*', cors({
  origin: ['https://文章子域名.com'],   // 只允许自己的前端域
  allowMethods: ['GET', 'POST', 'PUT', 'DELETE'],
  allowHeaders: ['Content-Type', 'Authorization'],
}))
```

---

## 七、技术决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| 渲染方案 | SSG（方案B）而非纯 SPA | GitHub Pages 无 native fallback；SSG 产出真实 HTML，SEO 权重完整 |
| 前端框架 | Vike + Vue3 + Vite7 | 保持纯 Vite+Vue 技术栈，Vike 仅加 SSG 能力，官方支持 GitHub Pages |
| 语言 | TypeScript | 编译期类型检查、重构安全、前后端共享类型契约 |
| 后端框架 | Hono | 轻量、边缘友好、TS 原生支持 |
| 构建位置 | GitHub Actions 云端 | 2G 内存服务器不扛构建；push 即自动构建发布 |
| 存储 | Markdown（content/）+ SQLite | 文章以 Markdown 文件形式存 git，天然版本化；后端数据用 SQLite 省内存 |
| 仓库 | 单仓库 monorepo | 前端+后端+内容+workflow 一处管理 |
| 部署域名 | 子域名 CNAME | GitHub Pages 无法绑主域到服务器场景下最省事，DNS 记录可共存 |

---

## 八、边界与安全

- 后端只监听 `127.0.0.1:3000`，不直接暴露公网，必须走 nginx 反代
- CORS 白名单只放行自己的文章域名
- Actions 只用官方 `actions/*`，不引用第三方 action，防供应链风险
- 服务器不跑构建、不跑前端，仅后端 + nginx，内存占用可控
- 私密配置（数据库口令等）用环境变量，不入 git

---

## 九、待办 / 下一步

- [ ] 确认本总汇后初始化仓库骨架（frontend/server/content/workflow）
- [ ] 用户提供：文章子域名（用于 CNAME 和 CORS 白名单）
- [ ] 用户提供：阿里云服务器 IP / SSH 接入（后端部署阶段用）
- [ ] 首篇示例文章（验证端到端链路）
